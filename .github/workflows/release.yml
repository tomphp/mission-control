name: Release

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  test:
    name: build and test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install web dependencies
        run: cd web && bun install --frozen-lockfile

      - name: Test
        run: make test

  tag:
    name: tag release
    needs: test
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.cog.outputs.version }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      # cog bump fails when there are no feat/fix commits since the last
      # tag (e.g. a docs/chore-only push). continue-on-error lets that
      # be treated as "no release needed" instead of failing the run -
      # every downstream job checks `needs.tag.outputs.version != ''`.
      - name: Bump version and tag
        id: cog
        continue-on-error: true
        uses: cocogitto/cocogitto-action@v3
        with:
          release: true
          git-user: 'Cog Bot'
          git-user-email: 'cog-bot@users.noreply.github.com'

      # Makes the outcome visible in the job summary instead of just a
      # green check - continue-on-error above hides real bump failures
      # the same way it hides the legitimate "nothing to bump" case.
      - name: Report bump result
        if: always()
        run: |
          if [ -n "${{ steps.cog.outputs.version }}" ]; then
            echo "Released \`${{ steps.cog.outputs.version }}\`." >> "$GITHUB_STEP_SUMMARY"
          else
            echo "No release: either there were no bumpable commits since the last tag, or the bump step failed - check the \"Bump version and tag\" step log above." >> "$GITHUB_STEP_SUMMARY"
          fi

  build-web:
    name: build web assets
    needs: tag
    if: needs.tag.outputs.version != ''
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ needs.tag.outputs.version }}

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Build web bundle
        run: cd web && bun install --frozen-lockfile && bun run build

      - uses: actions/upload-artifact@v7
        with:
          name: web-dist
          path: web/dist
          retention-days: 1

  build:
    name: build ${{ matrix.goos }}/${{ matrix.goarch }}
    needs: [tag, build-web]
    if: needs.tag.outputs.version != ''
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
          - goos: windows
            goarch: amd64
          - goos: windows
            goarch: arm64
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ needs.tag.outputs.version }}

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - uses: actions/download-artifact@v8
        with:
          name: web-dist
          path: web/dist

      - name: Build binary
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: 0
        run: |
          mkdir -p dist
          out="mission-control"
          if [ "${{ matrix.goos }}" = "windows" ]; then out="mission-control.exe"; fi
          go build -trimpath -ldflags "-s -w" -o "dist/${out}" ./cmd/mission-control

      - name: Package archive
        run: |
          name="mission-control_${{ needs.tag.outputs.version }}_${{ matrix.goos }}_${{ matrix.goarch }}"
          mkdir -p out
          if [ "${{ matrix.goos }}" = "windows" ]; then
            (cd dist && zip -q "../out/${name}.zip" mission-control.exe)
          else
            tar -czf "out/${name}.tar.gz" -C dist mission-control
          fi

      - uses: actions/upload-artifact@v7
        with:
          name: mission-control_${{ matrix.goos }}_${{ matrix.goarch }}
          path: out/*
          retention-days: 1

  publish:
    name: publish release
    needs: [tag, build]
    if: needs.tag.outputs.version != ''
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
          ref: ${{ needs.tag.outputs.version }}

      - uses: actions/download-artifact@v8
        with:
          pattern: mission-control_*
          path: out
          merge-multiple: true

      - name: Generate changelog
        uses: cocogitto/cocogitto-action@v3
        with:
          check: false

      - name: Generate changelog body
        run: cog changelog --at "${{ needs.tag.outputs.version }}" -t full_hash > CHANGELOG_body.md

      - name: Create GitHub release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ needs.tag.outputs.version }}
          body_path: CHANGELOG_body.md
          files: out/*
